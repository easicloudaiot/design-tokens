name: Notify consumers

# Public, source-only package consumed directly via git deps:
#   FE:     "@easicloudaiot/design-tokens": "github:easicloudaiot/design-tokens#main"
#   Mobile: tool/sync_shared.dart pulls tokens/*.json at a pinned SHA
#
# This workflow just fires a repository_dispatch at the consumers so each one
# can decide how to re-pull from main (open a bump PR, regenerate ARB/.g.dart,
# etc.). No npm registry publishing.

on:
  push:
    branches: [main]
    paths:
      - 'tokens/**'
      - 'src/**'
      - 'package.json'
  workflow_dispatch:

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/create-github-app-token@v1
        id: app-token
        with:
          app-id: ${{ secrets.BOT_APP_ID }}
          private-key: ${{ secrets.BOT_PRIVATE_KEY }}
          owner: easicloudaiot
          repositories: |
            EASIAioT-FE
            EASIAIoT-Mobile

      - name: Notify EASIAioT-FE
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
          SHA: ${{ github.sha }}
        run: |
          gh api repos/easicloudaiot/EASIAioT-FE/dispatches \
            -f event_type=design-tokens-updated \
            -F "client_payload[sha]=$SHA"

      - name: Notify EASIAIoT-Mobile
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
          SHA: ${{ github.sha }}
        run: |
          gh api repos/easicloudaiot/EASIAIoT-Mobile/dispatches \
            -f event_type=design-tokens-updated \
            -F "client_payload[sha]=$SHA"
